# Wave 2026-09-04

Source: carry-over from wave 2026-09-01 (closed 2026-09-04) plus the defects that wave's
monitoring revival surfaced. Orchestrator boots from this file under framework.md version 2.
Status column is maintained by the orchestrator.

## Manifest

| Ticket | Disposition | Status |
|--------|-------------|--------|
| SMK-486 ATS poller provider widening | Ready (Chris 2026-09-02, WS2-15 covers poller side, Jira comment 10972); brief carried from wave 1 | in review (PR #510; preflight passed; migration 184 written NOT applied, awaiting Chris; must apply staging then prod BEFORE this deploys; WS2-18/16.3.3 missing from canonical plan, flagged) |
| SMK-445 EMI KPI defects 1-5 | Ready (Chris 2026-09-02, scope per Jira comment 10973); brief carried from wave 1 | in review (PR #511; migration renumbering 184 to 185 after collision with PR 510; both migrations held for Chris, apply order 184 then 185, each BEFORE its PR deploys) |
| SMK-494 docs:index CRLF false-stale hooks | Ready (orchestrator call under standing approval; taxed every wave-1 worker) | queued |
| SMK-497 prod briefing page 7.8-8.4s vs 5s baseline | Ready to investigate (fix follows evidence); also blocks SMK-451 cascade closeout | queued after a slot opens |
| SMK-496 e2e dashboard contract | Held: Rich/Chris decision on rewriting specs against the simplified dashboard | held |
| SMK-478 scan outcome visibility | Held: Rich on UX states + WS2-16 scope (unchanged from wave 1) | held |
| SMK-485 render yield comparison | Tracking: run the comparison vs the 138/142 baseline 24-48h AFTER the promoted commit actually serves on prod | blocked on deploy |
| Prod deploy of promoted commit 7e73a5c2 | Escalated: Railway deploy 2026-09-03 21:41Z was SKIPPED, prod still on 63b5144c; redeploy needs Chris/Rich | escalated |

Coordination note: Teddy (trothschild1) is actively landing alerting work (PRs 505, 507, 508
merged; 509 open, counts against the Rich PR limit). Workers must not touch
.github/workflows alerting/incident files this wave; report conflicts instead.

WIP plan: start SMK-486 + SMK-445 (2 slots; Teddy's PR 509 occupies one Rich slot). Pull
SMK-494 then SMK-497 as slots open.

---

## Kickoff: SMK-486 ATS poller provider widening

You are a worker agent for ticket SMK-486 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: worker/signals/fetch-ats-json.js probes only greenhouse, lever and ashby, so 55 watchlist
companies are recorded not_found after an incomplete search; worker/scanner/ats-adapters.js knows
five providers. Implement the ticket's three items: widen the prober's provider list to match the
adapters, re-probe rows whose not_found predates the widening (exactly once), and record the
provider set a not_found was decided against. Fetch SMK-486 from Jira; it is the specification
including acceptance criteria and evidence tables.

Decisions already made (do not relitigate):
- WS2-15 covers the poller-side widening; no new story needed (Chris, 2026-09-02, Jira comment
  10972 on the ticket).
- Scope boundary stands: do not make the scanner read ats_boards (that is WS2-18, not this ticket).

Governance: AGENTS.md signal-engine preflight REQUIRED before changing code. Governing story
WS2-15. Step 2 of the preflight is pre-resolved by the Jira comment; complete the remaining steps,
record conclusions in your report, stop on any locked-boundary conflict.

Constraints:
- If recording the decided-against provider set needs a schema change: write the migration
  (next number in supabase/migrations/), include it in the PR, DO NOT apply it anywhere without
  orchestrator approval.
- Re-probe is a code/job change; do not run bulk probes against providers from your machine
  beyond a small smoke test.
- Do not touch .github/workflows alerting/incident files (Teddy's active lane).

Branch: SMK-486/ats-provider-widening
Definition of done: the ticket's acceptance list, including the parity test that fails when the
two provider lists diverge; tsc/lint and targeted test evidence. The before/after active-board
count is post-deploy evidence; note it as remaining gate state.
Escalate if: preflight surfaces a boundary conflict, any migration is ready to apply, or
smartrecruiters/bamboohr/workday probing needs credentials or endpoints that do not exist yet.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions. Per framework v2: never hold background watchers;
end turns with findings plus run IDs and the orchestrator re-dispatches you.

---

## Kickoff: SMK-445 EMI KPI metric correctness defects

You are a worker agent for ticket SMK-445 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: fix the EMI KPI defects. Fetch SMK-445 from Jira including comments; the ticket is the
specification and the 2026-09-02 comment (id 10973) records the scope decisions.

Decisions already made (do not relitigate; Chris, 2026-09-02, Jira comment 10973):
- Implement defects 1 through 5: source assessment completion from user_events, replace
  pull-all-rows client-side counting with server-side aggregates, fixed weekly cohorts with
  7-day maturity for day7_return, exclude synthetic/monitor accounts from all denominators
  (prod-probe@startingmonday.app and any account tagged synthetic), and insufficient_data
  status below a floor of 20.
- Defect 6: remove proof_assets_published_count and the b2b prospect metric from the automated
  pass/fail gate entirely; they are tracked manually from now on. Do not build data-entry flows.
- Targets: keep computing and snapshotting the remaining metrics weekly; downgrade the automated
  red/green gate to advisory/reporting until a re-baseline happens (4 to 8 weeks of clean data,
  then quarterly review). Do not invent new targets.

Governance: none (KPI reporting jobs, not signal-engine scope). Not MandateSignal; SM only.

Constraints:
- The metric_status constraint widening on emi_kpi_snapshots is a migration: write it (next free
  number in supabase/migrations/), include it in the PR, DO NOT apply anywhere without
  orchestrator approval.
- Historical snapshots: annotate rather than rewrite, per the ticket's acceptance list.
- Do not touch .github/workflows alerting/incident files (Teddy's active lane).

Branch: SMK-445/emi-kpi-correctness
Definition of done: the ticket's acceptance criteria as amended by comment 10973, demonstrated by
unit tests on the aggregate queries and cohort logic; no metric can exceed 100 percent; tsc/lint
evidence. Remaining gate state: migration apply (orchestrator), and the re-baseline itself happens
after the clean-data window, not in this PR.
Escalate if: server-side aggregates need a new RPC/view whose shape is ambiguous, any migration is
ready to apply, or excluding synthetic accounts requires a tagging scheme that does not exist.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions. Per framework v2: never hold background watchers;
end turns with findings plus run IDs and the orchestrator re-dispatches you.

---

## Kickoff: SMK-494 docs:index freshness check CRLF false-stale

You are a worker agent for ticket SMK-494 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: on Windows checkouts the pre-commit docs:index freshness check reports stale when the
regenerated file is byte-identical after line-ending normalization, and it also fails when
gitignored guide artifacts (docs/internal-guide.*) have not been generated locally. Every wave-1
worker lost time to this. Fetch SMK-494 from Jira for symptoms, suspected cause, and fix
direction. Likely files: scripts/generate-docs-index.mjs (--check path) and whatever the
pre-commit hook invokes around guide syncing.

Decisions already made (do not relitigate):
- Fix the check, not the docs: normalize line endings (or compare git-hashed content) before
  comparing, and make the check tolerate or regenerate the gitignored artifacts it depends on
  (orchestrator under standing approval, 2026-09-04).

Governance: none (developer tooling).

Constraints:
- Do not weaken what the check verifies (a genuinely stale index must still fail).
- Reproduce the false-stale on this machine first; your worktree IS the repro environment.
- Do not touch .github/workflows alerting/incident files (Teddy's active lane).

Branch: SMK-494/docs-index-crlf-check
Definition of done: on a fresh Windows worktree, the check passes with no regeneration step and
still fails when the index is genuinely stale (demonstrate both); tsc/lint evidence; PR to main,
reviewer richrothschild, body Closes SMK-494.
Escalate if: the fix requires changing git attributes/line-ending policy repo-wide rather than
the check itself.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions. Per framework v2: never hold background watchers;
end turns with findings plus run IDs and the orchestrator re-dispatches you.

---

## Kickoff: SMK-497 Production briefing page exceeds its 5s load baseline

You are a worker agent for ticket SMK-497 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: the Dashboard Behavior Baseline Agent measured /dashboard/briefing at 7820ms and 8437ms
against a 5000ms baseline in consecutive runs (both against prod build 63b5144c). Fetch SMK-497
from Jira. Investigation FIRST: profile where the time goes (server data aggregation vs render vs
network) before writing any fix. The briefing route aggregates signals/briefing data and scan
volume grew substantially in August; suspect a missing index or N+1.

Decisions already made (do not relitigate):
- Fix the root cause; do not widen the 5000ms threshold without understanding the cause
  (recorded on the ticket).

Governance: none expected (dashboard performance); BUT if the root cause reaches into scanner or
signals data models, stop and run the AGENTS.md signal-engine preflight before changing that code.

Constraints:
- Read-only profiling against prod data is fine (SELECTs, EXPLAIN); any schema change (index)
  is a migration: write it, include in PR, DO NOT apply without orchestrator approval.
- Reproduce locally in a prod-mode build before and after the fix; prod-verifiable evidence
  only lands post-deploy, note it as remaining gate state.
- Do not touch .github/workflows alerting/incident files (Teddy's active lane).

Branch: SMK-497/briefing-load-time
Definition of done: cause identified with profiling evidence; fix implemented with before/after
local measurements; tsc/lint and targeted test evidence; PR to main, reviewer richrothschild,
body Closes SMK-497. Remaining gate state: Baseline agent green on prod after deploy, which then
unblocks the SMK-451 cascade closeout (Reliability Daily Report + Monitoring Watchdog dispatch).
Escalate if: the cause is data volume requiring a product decision (e.g. pagination), any
migration is ready to apply, or the preflight trigger above fires.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions. Per framework v2: never hold background watchers;
end turns with findings plus run IDs and the orchestrator re-dispatches you.

---

## Outcomes

(filled by the orchestrator at wave close, with a short retro into framework.md)
