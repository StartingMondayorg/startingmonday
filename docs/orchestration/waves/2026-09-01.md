# Wave 2026-09-01

Source: Friday backlog grooming (Aug 28) plus the SMK-485 investigation executed 2026-09-01.
Orchestrator boots from this file. Status column is maintained by the orchestrator.

## Manifest

| Ticket | Disposition | Status |
|--------|-------------|--------|
| SMK-487 staging flag parity | Ready now, worker | done (Jira Done 2026-09-01; parity verified by worker and orchestrator, no PR needed) |
| SMK-451 monitoring workflows + Slack alerts | Ready now, worker | in review, complete pre-merge (PR #498, 7 commits, all agents green on branch incl Trust Integrity 3/3/3 parity; cascades verify post-merge; PGM residual = SMK-490 only) |
| SMK-489 extraction contract fix (from SMK-485) | Ready now, worker | done (PR #497 merged 2026-09-02 21:57Z, Jira Done; SMK-485 yield comparison pending 24-48h of post-deploy scan data) |
| SMK-486 ATS poller provider widening | Ready (Chris 2026-09-02: WS2-15 covers poller side, Jira comment 10972) | queued |
| SMK-445 EMI KPI defects 1-5 | Ready (Chris 2026-09-02: seeded metrics dropped from gate, re-baseline plan endorsed, Jira comment 10973) | queued |
| SMK-478 scan outcome visibility | Held: Rich on UX states + WS2-16 scope extension or new story before code | held |
| SMK-485 render yield spike | In Progress: investigation done, stays open for landing-page capture split + post-fix comparison (owned by SMK-489 worker) | tracking |
| SMK-490 CSP blocks theme-init script on funnel pages (found by SMK-451 monitoring) | Ready now (Chris, 2026-09-02), worker | in review (PR #499, nonce passed to next-themes, 5 routes verified clean locally; post-deploy prod check + PGM re-dispatch pending) |
| SMK-493 dependency audit red repo-wide (new advisories, incl. main) | New 2026-09-02; needs Chris scope call on the lighthouse-chain handling | held |
| SMK-491 duplicate main landmarks on /dashboard + /dashboard/signals (found by SMK-451 monitoring) | Ready (Chris 2026-09-02), worker; after SMK-490 | queued |
| SMK-492 dashboard CTA 17px tap target (found by SMK-451 monitoring) | Ready (Chris 2026-09-02), worker; after SMK-491 | queued |
| SMK-480 / SMK-481 auth pair | Deferred: fold 481 into 480, Rich-assigned | deferred |
| SMK-410 first-mile KPIs | Deferred: blocked in practice by SMK-445 | deferred |

WIP plan: start SMK-487 + SMK-451 + SMK-489 (3 slots). As slots free, pull SMK-486 then SMK-445
once Chris confirms their scope notes above.

Queue order as of 2026-09-02 (all Chris-confirmed, kickoff gated on the 2-PRs-on-Rich limit):
SMK-490, SMK-491, SMK-492, SMK-486, SMK-445.

---

## Kickoff: SMK-487 Staging renders a different landing hero than production

You are a worker agent for ticket SMK-487 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: the new staging environment renders a different hero and dashboard than production
because two NEXT_PUBLIC feature flags are 0 on staging and 1 on production. Set them on staging,
redeploy, and verify parity. This is a Railway config change; no repo code change is expected,
so the "branch and PR" lifecycle steps do not apply unless you end up changing code.
Full ticket text: fetch SMK-487 from Jira before starting; it names the exact service, environment,
variables, and acceptance check.

Decisions already made (do not relitigate):
- Proceed without Rich review; the wider "should staging default to prod flags" policy question is
  noted for Rich separately and is not this ticket (Chris, 2026-09-01).

Governance: none (environment config, not signal-engine scope).

Constraints:
- Railway MCP writes fail; use the Railway CLI (`railway variables --set ...`) or ask the
  orchestrator if CLI auth is missing.
- NEXT_PUBLIC_ vars require a redeploy to take effect in the client bundle.

Definition of done: staging and production return the same hero variant for the same commit,
shown by diffing the rendered hero block markup from both hosts (curl output lines in the report).
Comment the verification evidence on SMK-487 and transition it to Done (config tickets have no PR;
get orchestrator confirmation before the Done transition).
Escalate if: the staging service or environment names do not match the ticket, or a redeploy fails.

Report format: what changed, evidence lines (commands + key output), Verified/Unverified labels,
open questions.

---

## Kickoff: SMK-451 Scheduled monitoring workflows failing; alerts silently skipped

You are a worker agent for ticket SMK-451 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: several scheduled GitHub Actions reporting/monitoring workflows have been failing, and the
failure-alert step logs "No Slack webhook configured; skipping failure alert", so nothing reaches
Slack. Two workstreams: fix the root-cause workflows, and wire the failure alerts to the existing
webhook secrets.
Full ticket text and scope confirmation: fetch SMK-451 from Jira including comments; the 2026-08-16
comment fixes scope (webhook secrets SLACK_WEBHOOK_URL / SLACK_UI_ALERTS_WEBHOOK already exist in
the repo, the workflows just never reference them; consolidation ideas belong to SMK-469, not here).

Decisions already made (do not relitigate):
- Scope per the 2026-08-16 comment (Chris). Reporting consolidation is SMK-469, out of scope.

Governance: none (CI workflows, not signal-engine scope).

First step: re-verify which workflows are still red before fixing anything; the set has shifted
since filing (`gh run list --workflow <name> --limit 5` per workflow named on the ticket).
Fix root causes, not symptoms: Reliability Daily Report and Monitoring Watchdog are cascades of the
underlying agents.

Branch: SMK-451/monitoring-alerts
Definition of done: previously-red workflows pass on a manually dispatched run (evidence: run URLs);
every scheduled monitoring workflow's failure-alert step references the webhook secret; one test
alert fires into Slack via a deliberately failing dispatch or a dry-run step (draft the wording,
do not post anything to Slack yourself outside that webhook path).
Out of scope: consolidating reports (SMK-469), any change to what the reports measure.
Escalate if: a root cause sits in application code rather than workflow config, or a workflow's
purpose is unclear enough that fixing it means guessing.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions.

---

## Kickoff: SMK-489 Scanner extraction collapses rendered DOM into one line

You are a worker agent for ticket SMK-489 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: the scanner's text extraction depends on source whitespace, so rendered (and minified) pages
collapse into one line that role detection cannot read, and those scans are recorded as successes.
Implement the five-item scope on the ticket: browser-computed text at render time,
whitespace-independent extraction, extraction-failure outcome instead of success-zero,
per-scan text-shape telemetry, and acquisition-path recording in the live-brief scan writer.
Full ticket text: fetch SMK-489 from Jira before starting; it is the specification, including
acceptance criteria and the out-of-scope list (SMK-476, SMK-486, SMK-472 own the neighbors).
Evidence basis and baseline numbers: the 2026-09-01 comment on SMK-485.

Decisions already made (do not relitigate):
- Holistic fix, not a spot fix for known failing companies (Chris, 2026-09-01).
- Scope split across SMK-476/486/472 as recorded on the ticket (Chris, 2026-09-01).

Governance: AGENTS.md signal-engine preflight REQUIRED before changing code. Governing story
WS2-15 (scanner page-acquisition economics). Complete the preflight steps in AGENTS.md, record the
conclusions in your report, and stop on any conflict with a locked boundary. At handoff report
story ID, evidence produced, checks run, and remaining gate state.

Constraints:
- If scope item 3 needs a scan_results status/constraint change: write the migration file
  (next number in supabase/migrations/), include it in the PR, but DO NOT apply it anywhere
  without orchestrator approval (staging first, then prod, per CLAUDE.md).
- Key files: worker/scanner/extract-text.js, detect-roles.js, fetch-page.js, scan-company.js,
  write-results.js, live-brief-scan.js. Worker deps: `npm install` inside worker/.
- Do not change detection keywords or scoring; this ticket is the extraction contract only.

Branch: SMK-489/extraction-contract
Definition of done: the ticket's acceptance list, demonstrated by unit tests (minified-DOM fixture
end to end) plus tsc/lint evidence. The post-deploy yield comparison happens after merge and is not
part of this PR; note it as remaining gate state.
Escalate if: the browserless API change needs a plan/endpoint decision with cost implications,
any migration is ready to apply, or preflight surfaces a boundary conflict.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions.

---

## Kickoff: SMK-490 Production CSP blocks the theme-init inline script on funnel pages

You are a worker agent for ticket SMK-490 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: production's Content Security Policy blocks an inline script on /, /login, /onboarding,
/pricing and /signup because it carries no nonce. The blocked script is identified on the ticket:
the 526-char theme initialization snippet (pre-paint light/dark setter), hash
sha256-J9cZHZf5nVZbsm7Pqxc8RsURv1AIXkMgbhfrZvoOs/A=. The Next.js framework scripts on the same
pages DO carry the nonce, so the nonce plumbing works; the theme snippet bypasses it, most likely
injected by a theme provider. Full details and evidence: fetch SMK-490 from Jira including comments.

Decisions already made (do not relitigate):
- Fix in this wave (Chris, 2026-09-02).
- Preferred fix: route the theme-init script through the same nonce mechanism the framework
  scripts use. Adding the script hash to the CSP is the fallback if nonce attachment is not
  feasible; changing the policy any other way needs escalation.

Governance: none (frontend CSP/nonce fix, not signal-engine scope).

Constraints:
- Do not weaken the CSP (no unsafe-inline, no wildcard sources).
- Do not suppress or change the monitoring console-error check; SMK-451's harness correctly
  flags this until fixed.
- Find where the CSP and nonce are generated (likely middleware or next.config) and where the
  theme provider injects its script before proposing the change.

Branch: SMK-490/theme-script-csp-nonce
Definition of done: locally reproduced then fixed (the theme script carries the nonce or its hash
is in the policy); no CSP console error on the five routes in a local prod-mode run; existing
theme behavior unchanged (pre-paint theme set, no flash); tsc/lint evidence. Note in the report
that post-deploy verification on prod is remaining gate state, plus a PGM re-dispatch under
SMK-451 to confirm the 5 residual failures clear.
Escalate if: the theme script comes from a third-party package that cannot receive the nonce
without patching, or the fix requires changing the CSP beyond adding this one script.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions.

---

## Kickoff: SMK-491 Duplicate main landmarks on /dashboard and /dashboard/signals

You are a worker agent for ticket SMK-491 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: RE-SCOPED 2026-09-02 (see the ticket's comment 10974). The settled markup is fine; the
violation is loading-state only: seven dashboard loading.tsx files declare their own main element,
so during Suspense streaming the DOM holds two main landmarks, violating the addendum clause that
covers hidden/loading states. Fetch SMK-491 from Jira including comments; comment 10974 lists the
seven files and the verification method. The page.tsx branches on /dashboard are mutually
exclusive and must NOT be changed.

Decisions already made (do not relitigate):
- Fix in this wave, after SMK-490 (Chris, 2026-09-02).
- Scope per comment 10974: loading-file landmark swap only, no layout redesign.

Governance: none (dashboard markup, not signal-engine scope).

Constraints:
- Swap main for a non-landmark wrapper in the seven loading files, preserving aria-busy/aria-live
  and all styling. Touch nothing else.

Branch: SMK-491/single-main-landmark
Definition of done: exactly one main landmark per dashboard route in loading state (mid-stream DOM
capture or targeted test) and Trust Integrity Agent still green on a branch dispatch; tsc/lint
evidence.
Escalate if: any loading file's main carries behavior beyond landmark semantics that a wrapper
swap would break.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions.

---

## Kickoff: SMK-492 Dashboard CTA below mobile tap-target minimum

You are a worker agent for ticket SMK-492 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: the Weekly Mobile UX Audit workflow is red because a dashboard CTA measures 17px tall
against the 32px mobile tap-target rubric minimum. Fetch SMK-492 from Jira. First identify the
exact CTA from the latest Weekly Mobile UX Audit run artifacts/logs, then fix its sizing.

Decisions already made (do not relitigate):
- Fix in this wave, after SMK-491 (Chris, 2026-09-02).

Governance: none.

Constraints:
- Follow the frontend-components conventions (repo skill) for any UI edit.
- Fix the component sizing; do not relax the audit rubric threshold.

Branch: SMK-492/dashboard-cta-tap-target
Definition of done: the CTA meets the 32px minimum on mobile viewport, shown by a local
measurement or the audit script run locally if it supports it; tsc/lint evidence; mobile visual
gates pass in pre-commit. Note as remaining gate state: post-merge Weekly Mobile UX Audit dispatch.
Escalate if: the 17px CTA is a design-system primitive whose resize would affect many surfaces.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions.

---

## Kickoff: SMK-486 ATS poller provider widening

You are a worker agent for ticket SMK-486 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: worker/signals/fetch-ats-json.js probes only greenhouse, lever and ashby, so 55 watchlist
companies are recorded not_found after an incomplete search; worker/scanner/ats-adapters.js knows
five providers. Implement the ticket's three items: widen the prober's provider list to match the
adapters, re-probe rows whose not_found predates the widening (exactly once), and record the
provider set a not_found was decided against. Fetch SMK-486 from Jira; it is the specification
including acceptance criteria and evidence tables.

Decisions already made (do not relitigate):
- WS2-15 covers the poller-side widening; no new story needed (Chris, 2026-09-02, Jira comment
  10972 on the ticket).
- Scope boundary stands: do not make the scanner read ats_boards (that is WS2-18, not this ticket).

Governance: AGENTS.md signal-engine preflight REQUIRED before changing code. Governing story
WS2-15. Step 2 of the preflight is pre-resolved by the Jira comment; complete the remaining steps,
record conclusions in your report, stop on any locked-boundary conflict.

Constraints:
- If recording the decided-against provider set needs a schema change: write the migration
  (next number in supabase/migrations/), include it in the PR, DO NOT apply it anywhere without
  orchestrator approval.
- Re-probe is a code/job change; do not run bulk probes against providers from your machine
  beyond a small smoke test.

Branch: SMK-486/ats-provider-widening
Definition of done: the ticket's acceptance list, including the parity test that fails when the
two provider lists diverge; tsc/lint and targeted test evidence. The before/after active-board
count is post-deploy evidence; note it as remaining gate state.
Escalate if: preflight surfaces a boundary conflict, any migration is ready to apply, or
smartrecruiters/bamboohr/workday probing needs credentials or endpoints that do not exist yet.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions.

---

## Kickoff: SMK-445 EMI KPI metric correctness defects

You are a worker agent for ticket SMK-445 in the Starting Monday repo. Work only this ticket.
Read docs/orchestration/framework.md sections "Worker lifecycle" and "Guardrails" and follow them.

Ticket: fix the EMI KPI defects. Fetch SMK-445 from Jira including comments; the ticket is the
specification and the 2026-09-02 comment (id 10973) records the scope decisions.

Decisions already made (do not relitigate; Chris, 2026-09-02, Jira comment 10973):
- Implement defects 1 through 5: source assessment completion from user_events, replace
  pull-all-rows client-side counting with server-side aggregates, fixed weekly cohorts with
  7-day maturity for day7_return, exclude synthetic/monitor accounts from all denominators
  (prod-probe@startingmonday.app and any account tagged synthetic), and insufficient_data
  status below a floor of 20.
- Defect 6: remove proof_assets_published_count and the b2b prospect metric from the automated
  pass/fail gate entirely; they are tracked manually from now on. Do not build data-entry flows.
- Targets: keep computing and snapshotting the remaining metrics weekly; downgrade the automated
  red/green gate to advisory/reporting until a re-baseline happens (4 to 8 weeks of clean data,
  then quarterly review). Do not invent new targets.

Governance: none (KPI reporting jobs, not signal-engine scope). Not MandateSignal; SM only.

Constraints:
- The metric_status constraint widening on emi_kpi_snapshots is a migration: write it (next free
  number in supabase/migrations/), include it in the PR, DO NOT apply anywhere without
  orchestrator approval.
- Historical snapshots: annotate rather than rewrite, per the ticket's acceptance list.

Branch: SMK-445/emi-kpi-correctness
Definition of done: the ticket's acceptance criteria as amended by comment 10973, demonstrated by
unit tests on the aggregate queries and cohort logic; no metric can exceed 100 percent; tsc/lint
evidence. Remaining gate state: migration apply (orchestrator), and the re-baseline itself happens
after the clean-data window, not in this PR.
Escalate if: server-side aggregates need a new RPC/view whose shape is ambiguous, any migration is
ready to apply, or excluding synthetic accounts requires a tagging scheme that does not exist.

Report format: what changed, evidence lines (commands + key output), PR URL,
Verified/Unverified labels, open questions.

---

## Outcomes

(filled by the orchestrator at wave close, with a short retro into framework.md)
